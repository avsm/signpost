;
; BIND data file for local loopback interface
;
$TTL	604800
@	IN	SOA	ns.sp. root.sp. (
			      9		; Serial
			 604800		; Refresh
			  86400		; Retry
			2419200		; Expire
			 604800 )	; Negative Cache TTL
;
@	IN	NS	ns.sp.
@	IN	A	10.10.0.3
ns      IN      A       10.10.0.3
